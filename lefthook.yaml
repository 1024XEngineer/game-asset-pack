pre-commit:
  commands:
    frontend-format:
      root: "frontend/"
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm format {staged_files}
      stage_fixed: true

    frontend-lint:
      root: "frontend/"
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm lint
