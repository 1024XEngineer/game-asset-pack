pre-commit:
  commands:
    backend-format:
      root: "core_api/"
      glob: "*.go"
      run: golangci-lint fmt {staged_files}
      stage_fixed: true

    backend-lint:
      root: "core_api/"
      glob: "*.go"
      run: golangci-lint run